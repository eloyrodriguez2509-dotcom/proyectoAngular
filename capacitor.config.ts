import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eloy.proyectoangular',
  appName: 'proyecto-angular',
  webDir: 'dist/proyectoAngular/browser'
};

export default config;